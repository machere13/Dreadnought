export interface InputCoreOptions {
  disabled?: boolean;
  readOnly?: boolean;
  required?: boolean;
  invalid?: boolean;
}

export interface InputCore {
  disabled: boolean;
  readOnly: boolean;
  required: boolean;
  invalid: boolean;
}
